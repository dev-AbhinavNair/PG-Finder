<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FindMyPG Owner - Bookings</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

  <style>
    body {
      background: #f5f7fb;
    }

    .topbar {
      background: #ffffff;
      border-bottom: 1px solid #e5e7eb;
    }

    .brand-dot {
      width: 28px;
      height: 28px;
      border-radius: 8px;
      background: #0ea5a4;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-weight: 700;
    }

    .sidebar {
      background: #f3f4f6;
      border-right: 1px solid #e5e7eb;
      min-height: calc(100vh - 56px);
      padding: 14px 10px;
    }

    .side-link {
      display: flex;
      align-items: center;
      gap: 10px;
      color: #374151;
      text-decoration: none;
      padding: 10px 12px;
      border-radius: 10px;
      font-size: .95rem;
    }

    .side-link:hover {
      background: #e9eefb;
    }

    .side-link.active {
      background: #e9eefb;
      color: #111827;
      font-weight: 600;
    }

    .side-ic {
      width: 18px;
      height: 18px;
      opacity: .75;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }

    .content-wrap {
      padding: 18px 18px 28px;
    }

    .page-title {
      font-weight: 700;
      font-size: 1.4rem;
      color: #111827;
    }

    .searchbar .form-control {
      border-radius: 12px;
      padding-left: 36px;
    }

    .search-icon {
      position: absolute;
      left: 12px;
      top: 50%;
      transform: translateY(-50%);
      opacity: .55;
    }

    .table-wrapper {
      background: #fff;
      border-radius: 14px;
      box-shadow: 0 8px 22px rgba(15, 23, 42, 0.08);
      overflow: hidden;
      margin-top: 18px;
    }

    .table {
      margin: 0;
    }

    table thead {
      background: #f9fafb;
      border-bottom: 1px solid #eef2f7;
    }

    table th {
      font-weight: 600;
      color: #374151;
      padding: 14px;
      font-size: .9rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    table td {
      padding: 14px;
      vertical-align: middle;
      color: #4b5563;
      font-size: .95rem;
      border-bottom: 1px solid #eef2f7;
    }

    table tbody tr:hover {
      background: #f9fafb;
    }

    .tenant-avatar {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background: #d1d5db;
      object-fit: cover;
    }

    .status-badge {
      padding: 4px 12px;
      border-radius: 999px;
      font-size: .8rem;
      font-weight: 500;
    }

    .status-confirmed {
      background: #dbeafe;
      color: #1e40af;
    }

    .status-pending {
      background: #fff7ed;
      color: #9a3412;
    }

    .status-checked_in {
      background: #dcfce7;
      color: #166534;
    }

    .status-completed {
      background: #f3e8ff;
      color: #6b21a8;
    }

    .status-cancelled {
      background: #fee2e2;
      color: #991b1b;
    }

    .payment-pending {
      background: #fef3c7;
      color: #92400e;
    }

    .payment-completed {
      background: #dcfce7;
      color: #166534;
    }

    .payment-failed {
      background: #fee2e2;
      color: #991b1b;
    }

    .muted {
      color: #6b7280;
    }

    .footer {
      color: #9ca3af;
      font-size: .85rem;
      padding: 18px;
      border-top: 1px solid #e5e7eb;
      background: #fff;
    }

    .sidebar-overlay {
      display: none;
    }

    @media (max-width: 991.98px) {
      .sidebar {
        position: fixed;
        top: 56px;
        bottom: 0;
        left: 0;
        width: 260px;
        transform: translateX(-100%);
        z-index: 1040;
        transition: transform 0.3s ease-in-out;
        box-shadow: 4px 0 24px rgba(0, 0, 0, 0.1);
      }

      .sidebar.show {
        transform: translateX(0);
      }

      .sidebar-overlay {
        display: block;
        position: fixed;
        top: 56px;
        bottom: 0;
        left: 0;
        right: 0;
        background: rgba(0, 0, 0, 0.4);
        z-index: 1030;
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s;
      }

      .sidebar-overlay.show {
        opacity: 1;
        visibility: visible;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar topbar navbar-expand-lg px-3" style="height:56px;">
    <div class="d-flex align-items-center gap-2">
      <button class="btn btn-light d-lg-none me-2" id="sidebarToggle">
        <i class="bi bi-list"></i>
      </button>
      <div class="brand-dot">🏠</div>
      <span class="fw-semibold" style="color:#0f766e;">FindMyPG Owner</span>
    </div>

    <div class="ms-auto d-flex align-items-center gap-3">
      <% if (typeof owner !=="undefined" && owner) { %>
        <span class="small text-secondary d-none d-md-inline">Hi, <%= owner.name %></span>
        <% } %>

          <div class="rounded-circle bg-light border" style="width:34px;height:34px; overflow:hidden;">
            <% if (typeof owner !=="undefined" && owner && owner.avatar_url) { %>
              <img src="<%= owner.avatar_url %>" alt="avatar" style="width:100%;height:100%;object-fit:cover;">
              <% } %>
          </div>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row g-0">
      <aside class="col-12 col-md-3 col-lg-2 sidebar">
        <a class="side-link" href="/owner">
          <span class="side-ic">▦</span> My PGs
        </a>
        <a class="side-link" href="/owner/pg/new">
          <span class="side-ic">＋</span> Add New PG
        </a>
        <a class="side-link active" href="/owner/bookings">
          <span class="side-ic">🗓</span> Bookings
        </a>
        <a class="side-link" href="/owner/messages">
          <span class="side-ic">💬</span> Messages
        </a>
        <a class="side-link" href="/owner/payouts">
          <span class="side-ic">₹</span> Payouts
        </a>
        <a class="side-link" href="/owner/settings">
          <span class="side-ic"><i class="bi bi-gear"></i></span> Settings
        </a>

        <form action="/logout" method="POST" class="mt-3 px-2">
          <button class="btn btn-outline-secondary w-100 btn-sm" type="submit">Logout</button>
        </form>
      </aside>
      <div class="sidebar-overlay" id="sidebarOverlay"></div>

      <main class="col-12 col-md-9 col-lg-10 content-wrap">
        <div class="page-title mb-4">Bookings</div>

        <% if (typeof success !=='undefined' && success) { %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= success %>
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
          <% } %>

            <% if (typeof error !=='undefined' && error) { %>
              <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error %>
                  <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
              </div>
              <% } %>

                <div class="row g-2 align-items-center mb-3">
                  <div class="col-12 col-lg-8">
                    <div class="position-relative searchbar">
                      <span class="search-icon">🔎</span>
                      <form action="/owner/bookings" method="GET">
                        <input class="form-control" type="text" name="q" placeholder="Search by Tenant or PG..."
                          value="<%= typeof q !== 'undefined' ? q : '' %>">
                      </form>
                    </div>
                  </div>
                  <div class="col-12 col-lg-4">
                    <form action="/owner/bookings" method="GET" class="d-flex gap-2">
                      <input type="hidden" name="q" value="<%= typeof q !== 'undefined' ? q : '' %>">
                      <select name="status" class="form-select" style="border-radius: 12px;">
                        <option value="">All Statuses</option>
                        <option value="pending" <%=status==='pending' ? 'selected' : '' %>>Pending</option>
                        <option value="confirmed" <%=status==='confirmed' ? 'selected' : '' %>>Confirmed</option>
                        <option value="checked-in" <%=status==='checked-in' ? 'selected' : '' %>>Checked In</option>
                        <option value="completed" <%=status==='completed' ? 'selected' : '' %>>Completed</option>
                        <option value="cancelled" <%=status==='cancelled' ? 'selected' : '' %>>Cancelled</option>
                      </select>
                      <button class="btn btn-outline-secondary" style="border-radius: 12px;"
                        type="submit">Apply</button>
                    </form>
                  </div>
                </div>

                <div class="table-wrapper">
                  <table class="table">
                    <thead>
                      <tr>
                        <th style="width: 180px;">Tenant</th>
                        <th style="width: 200px;">PG Details</th>
                        <th style="width: 200px;">Booking Dates</th>
                        <th style="width: 100px;">Duration</th>
                        <th style="width: 120px;">Status</th>
                        <th style="width: 100px;">Payment</th>
                        <th style="width: 220px;">Actions</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if (!bookings || bookings.length===0) { %>
                        <tr>
                          <td colspan="5" class="text-center py-4 muted">No bookings found.</td>
                        </tr>
                        <% } else { %>
                          <% bookings.forEach(b=> { %>
                            <tr>
                              <td>
                                <div style="display: flex; align-items: center; gap: 12px;">
                                  <img src="<%= b.tenant_avatar || 'https://via.placeholder.com/32' %>" alt="avatar"
                                    class="tenant-avatar">
                                  <div>
                                    <div style="font-weight: 500; color: #111827;">
                                      <%= b.tenant_name || 'Unnamed' %>
                                    </div>
                                    <div class="muted" style="font-size: .8rem;">
                                      <%= b.tenant_contact || 'N/A' %>
                                    </div>
                                  </div>
                                </div>
                              </td>
                              <td>
                                <div style="font-weight: 600;">
                                  <%= b.pg_name || 'PG' %>
                                </div>
                                <div class="muted" style="font-size: .85rem;">
                                  <%= b.room_type || 'Single' %>
                                </div>
                              </td>
                              <td>
                                <div style="font-size: .9rem; font-weight: 500;">
                                  <% const checkIn=new Date(b.check_in_date); %>
                                    <% const checkOut=b.check_out_date ? new Date(b.check_out_date) : null; %>
                                      <%= checkIn.toLocaleDateString('en-IN', { day: 'numeric' , month: 'short' ,
                                        year: 'numeric' }) %>
                                        <% if (checkOut) { %>
                                          <br>
                                          <span class="muted">to</span><br>
                                          <%= checkOut.toLocaleDateString('en-IN', { day: 'numeric' , month: 'short' ,
                                            year: 'numeric' }) %>
                                            <% } else { %>
                                              <span class="badge bg-warning">No end date</span>
                                              <% } %>
                                </div>
                                <div class="muted" style="font-size: .8rem; margin-top: 4px;">₹<%= b.monthly_rent %>/mo
                                </div>
                              </td>
                              <td>
                                <% if (checkOut) { %>
                                  <% const days=Math.ceil((checkOut - checkIn) / (1000 * 60 * 60 * 24)); %>
                                    <div style="font-size: .9rem; font-weight: 500;">
                                      <%= days %> days
                                    </div>
                                    <div class="muted" style="font-size: .8rem;">
                                      <%= Math.ceil(days / 30) %> month(s)
                                    </div>
                                    <% } else { %>
                                      <span class="muted">-</span>
                                      <% } %>
                              </td>
                              <td>
                                <span class="status-badge status-<%= b.booking_status %>">
                                  <%= b.booking_status.charAt(0).toUpperCase() +
                                    b.booking_status.slice(1).replace('_', ' ' ) %>
                                </span>
                              </td>
                              <td>
                                <span class="status-badge payment-<%= b.payment_status %>">
                                  <%= b.payment_status.charAt(0).toUpperCase() + b.payment_status.slice(1) %>
                                </span>
                              </td>
                              <td>
                                <div class="d-flex gap-1 flex-wrap">
                                  <button type="button" class="btn btn-sm btn-info"
                                    onclick="showBookingDetails('<%= b._id %>')">
                                    <i class="bi bi-eye"></i> Details
                                  </button>
                                </div>
                              </td>
                            </tr>
                            <% }) %>
                              <% } %>
                    </tbody>
                  </table>
                </div>
      </main>
    </div>
  </div>

  <div class="footer text-center">
    © <%= new Date().getFullYear() %> FindMyPG Owner. All rights reserved.
  </div>

  <div class="modal fade" id="bookingDetailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Booking Details</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body" id="bookingDetailsContent">
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    const bookingsData = <% - JSON.stringify(bookings) %>;

    function showBookingDetails(bookingId) {
      const booking = bookingsData.find(b => String(b._id) === String(bookingId));
      if (!booking) return;

      const checkIn = new Date(booking.check_in_date);
      const checkOut = booking.check_out_date ? new Date(booking.check_out_date) : null;
      const days = checkOut ? Math.ceil((checkOut - checkIn) / (1000 * 60 * 60 * 24)) : 0;

      const content = `
    <div class="row">
      <div class="col-md-6">
        <h6>Tenant Information</h6>
        <p><strong>Name:</strong> ${booking.tenant_name || 'N/A'}</p>
        <p><strong>Contact:</strong> ${booking.tenant_contact || 'N/A'}</p>
        <p><strong>Email:</strong> ${booking.tenant_id?.email || 'N/A'}</p>
      </div>
      <div class="col-md-6">
        <h6>Booking Information</h6>
        <p><strong>PG:</strong> ${booking.pg_name || 'N/A'}</p>
        <p><strong>Room Type:</strong> ${booking.room_type || 'N/A'}</p>
        <p><strong>Monthly Rent:</strong> ₹${booking.monthly_rent || 0}/mo</p>
      </div>
    </div>
    <hr>
    <div class="row">
      <div class="col-md-6">
        <h6>Booking Dates</h6>
        <p><strong>Check-in:</strong> ${checkIn.toLocaleDateString('en-IN', { day: 'numeric', month: 'long', year: 'numeric' })}</p>
        ${checkOut ? `<p><strong>Check-out:</strong> ${checkOut.toLocaleDateString('en-IN', { day: 'numeric', month: 'long', year: 'numeric' })}</p>` : '<p><strong>Check-out:</strong> Not set</p>'}
        ${days ? `<p><strong>Duration:</strong> ${days} days (${Math.ceil(days / 30)} months)</p>` : ''}
      </div>
      <div class="col-md-6">
        <h6>Status Information</h6>
        <p><strong>Booking Status:</strong> <span class="status-badge status-${booking.booking_status}">${booking.booking_status.charAt(0).toUpperCase() + booking.booking_status.slice(1).replace('_', ' ')}</span></p>
        <p><strong>Payment Status:</strong> <span class="status-badge payment-${booking.payment_status}">${booking.payment_status.charAt(0).toUpperCase() + booking.payment_status.slice(1)}</span></p>
        <p><strong>Booked on:</strong> ${new Date(booking.createdAt).toLocaleDateString('en-IN')}</p>
      </div>
    </div>
    ${booking.tenant_notes ? `
    <hr>
    <div class="row">
      <div class="col-12">
        <h6>Tenant Notes</h6>
        <p>${booking.tenant_notes}</p>
      </div>
    </div>
    ` : ''}
    ${booking.owner_notes ? `
    <hr>
    <div class="row">
      <div class="col-12">
        <h6>Owner Notes</h6>
        <p>${booking.owner_notes}</p>
      </div>
    </div>
    ` : ''}
  `;

      document.getElementById('bookingDetailsContent').innerHTML = content;

      const modal = new bootstrap.Modal(document.getElementById('bookingDetailsModal'));
      modal.show();
    }

    const toggleBtn = document.getElementById('sidebarToggle');
    const sidebar = document.querySelector('.sidebar');
    const overlay = document.getElementById('sidebarOverlay');

    if (toggleBtn && sidebar && overlay) {
      toggleBtn.addEventListener('click', () => {
        sidebar.classList.toggle('show');
        overlay.classList.toggle('show');
      });

      overlay.addEventListener('click', () => {
        sidebar.classList.remove('show');
        overlay.classList.remove('show');
      });
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>