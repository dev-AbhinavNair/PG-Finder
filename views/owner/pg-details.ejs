<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="icon" type="image/png" href="/media/favIcon.png">
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FindMyPG Owner - <%= pg.name %> Details</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

  <style>
    body {
      background: #f5f7fb;
    }

    .topbar {
      background: #ffffff;
      border-bottom: 1px solid #e5e7eb;
      height: 56px;
    }

    .brand-dot {
      width: 28px;
      height: 28px;
      border-radius: 8px;
      background: #0ea5a4;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-weight: 700;
    }

    .sidebar {
      background: #f3f4f6;
      border-right: 1px solid #e5e7eb;
      min-height: calc(100vh - 56px);
      padding: 14px 10px;
    }

    .side-link {
      display: flex;
      align-items: center;
      gap: 10px;
      color: #374151;
      text-decoration: none;
      padding: 10px 12px;
      border-radius: 10px;
      font-size: .95rem;
    }

    .side-link:hover {
      background: #e9eefb;
    }

    .side-link.active {
      background: #e9eefb;
      color: #111827;
      font-weight: 600;
    }

    .side-ic {
      width: 18px;
      height: 18px;
      opacity: .75;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }

    .content-wrap {
      padding: 18px 18px 28px;
    }

    .page-title {
      font-weight: 700;
      font-size: 1.4rem;
      color: #111827;
    }

    .stat-card {
      background: #fff;
      border-radius: 14px;
      padding: 20px;
      box-shadow: 0 8px 22px rgba(15, 23, 42, 0.08);
      border: 1px solid #f1f5f9;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .stat-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 28px rgba(15, 23, 42, 0.12);
    }

    .stat-value {
      font-size: 1.8rem;
      font-weight: 700;
      color: #111827;
    }

    .stat-label {
      color: #6b7280;
      font-size: 0.875rem;
      margin-top: 4px;
    }

    .pg-image {
      width: 100%;
      height: 240px;
      object-fit: cover;
      border-radius: 12px;
    }

    .thumbnail {
      width: 80px;
      height: 60px;
      object-fit: cover;
      border-radius: 8px;
      cursor: pointer;
      transition: opacity 0.2s ease;
    }

    .thumbnail:hover {
      opacity: 0.8;
    }

    .badge-status {
      font-size: 0.75rem;
      padding: 4px 8px;
      border-radius: 6px;
      font-weight: 500;
    }

    .booking-card {
      background: #fff;
      border-radius: 12px;
      padding: 16px;
      margin-bottom: 12px;
      border: 1px solid #f1f5f9;
      transition: box-shadow 0.2s ease;
    }

    .booking-card:hover {
      box-shadow: 0 4px 12px rgba(15, 23, 42, 0.06);
    }

    .amenity-tag {
      display: inline-block;
      padding: 6px 12px;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      font-size: 0.875rem;
      margin: 4px;
    }

    .sidebar-overlay {
      display: none;
    }

    @media (max-width: 991.98px) {
      .sidebar {
        position: fixed;
        top: 56px;
        bottom: 0;
        left: 0;
        width: 260px;
        transform: translateX(-100%);
        z-index: 1040;
        transition: transform 0.3s ease-in-out;
        box-shadow: 4px 0 24px rgba(0, 0, 0, 0.1);
      }

      .sidebar.show {
        transform: translateX(0);
      }

      .sidebar-overlay {
        display: block;
        position: fixed;
        top: 56px;
        bottom: 0;
        left: 0;
        right: 0;
        background: rgba(0, 0, 0, 0.4);
        z-index: 1030;
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s;
      }

      .sidebar-overlay.show {
        opacity: 1;
        visibility: visible;
      }
    }
  </style>
</head>

<body>
  <nav class="navbar topbar navbar-expand-lg px-3">
    <div class="d-flex align-items-center gap-2">
      <button class="btn btn-light d-lg-none me-2" id="sidebarToggle">
        <i class="bi bi-list"></i>
      </button>
      <div class="brand-dot">🏠</div>
      <span class="fw-semibold" style="color:#0f766e;">FindMyPG Owner</span>
    </div>

    <div class="ms-auto d-flex align-items-center gap-3">
      <% if (typeof owner !=="undefined" && owner) { %>
        <span class="small text-secondary d-none d-md-inline">Hi, <%= owner.name %></span>
        <% } %>

          <div class="rounded-circle bg-light border" style="width:34px;height:34px; overflow:hidden;">
            <% if (typeof owner !=="undefined" && owner && owner.avatar_url) { %>
              <img src="<%= owner.avatar_url %>" alt="avatar" style="width:100%;height:100%;object-fit:cover;">
              <% } %>
          </div>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row g-0">
      <aside class="col-12 col-md-3 col-lg-2 sidebar">
        <a class="side-link active" href="/owner">
          <span class="side-ic">▦</span> My PGs
        </a>
        <a class="side-link" href="/owner/pg/new">
          <span class="side-ic">＋</span> Add New PG
        </a>
        <a class="side-link" href="/owner/bookings">
          <span class="side-ic">🗓</span> Bookings
        </a>
        <a class="side-link" href="/owner/messages">
          <span class="side-ic">💬</span> Messages
        </a>
        <a class="side-link" href="/owner/payouts">
          <span class="side-ic">₹</span> Payouts
        </a>
        <a class="side-link" href="/owner/payouts/history">
          <span class="side-ic">📋</span> Payout History
        </a>
        <a class="side-link" href="/owner/settings">
          <span class="side-ic"><i class="bi bi-gear"></i></span> Settings
        </a>

        <form action="/logout" method="POST" class="mt-3 px-2">
          <button class="btn btn-outline-secondary w-100 btn-sm" type="submit">Logout</button>
        </form>
      </aside>
      <div class="sidebar-overlay" id="sidebarOverlay"></div>

      <main class="col-12 col-md-9 col-lg-10 content-wrap">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <div>
            <h1 class="page-title mb-1"><%= pg.name %></h1>
            <p class="text-muted mb-0"><%= pg.area %>, <%= pg.city %></p>
          </div>
          <div>
            <a href="/owner/pg/<%= pg._id %>/edit" class="btn btn-primary">
              <i class="bi bi-pencil"></i> Edit PG
            </a>
          </div>
        </div>

        <!-- Stats Overview -->
        <div class="row mb-4">
          <div class="col-md-2">
            <div class="stat-card">
              <div class="stat-value"><%= stats.totalBookings %></div>
              <div class="stat-label">Total Bookings</div>
            </div>
          </div>
          <div class="col-md-2">
            <div class="stat-card">
              <div class="stat-value">₹<%= (stats.totalRevenue).toLocaleString('en-IN') %></div>
              <div class="stat-label">Total Revenue</div>
            </div>
          </div>
          <div class="col-md-2">
            <div class="stat-card">
              <div class="stat-value"><%= stats.activeBookings %></div>
              <div class="stat-label">Active Stays</div>
            </div>
          </div>
          <div class="col-md-2">
            <div class="stat-card">
              <div class="stat-value"><%= stats.pendingBookings %></div>
              <div class="stat-label">Pending</div>
            </div>
          </div>
          <div class="col-md-2">
            <div class="stat-card">
              <div class="stat-value"><%= stats.completedBookings %></div>
              <div class="stat-label">Completed</div>
            </div>
          </div>
        </div>

        <div class="row">
          <!-- Left Column - PG Info & Gallery -->
          <div class="col-lg-8">
            <!-- PG Images -->
            <div class="stat-card mb-4">
              <h5 class="mb-3">Property Gallery</h5>
              <img src="<%= pg.coverUrl || '/media/default-pg.jpg' %>" class="pg-image mb-3" id="mainImage">
              <div class="d-flex gap-2 flex-wrap">
                <% pg.galleryUrls.forEach((url, index) => { %>
                  <img src="<%= url %>" class="thumbnail" onclick="changeMainImage('<%= url %>')">
                <% }) %>
              </div>
            </div>

            <!-- PG Details -->
            <div class="stat-card mb-4">
              <h5 class="mb-3">Property Details</h5>
              <div class="row">
                <div class="col-md-6">
                  <p><strong>Address:</strong> <%= pg.addressLine %></p>
                  <p><strong>Area:</strong> <%= pg.area %></p>
                  <p><strong>City:</strong> <%= pg.city %>, <%= pg.state %> - <%= pg.pincode %></p>
                  <p><strong>Gender:</strong> <%= pg.gender %></p>
                  <p><strong>Food Included:</strong> <%= pg.foodIncluded ? 'Yes' : 'No' %></p>
                </div>
                <div class="col-md-6">
                  <p><strong>Min Price:</strong> ₹<%= pg.minPrice %>/month</p>
                  <p><strong>Max Price:</strong> ₹<%= pg.maxPrice %>/month</p>
                  <p><strong>Status:</strong> 
                    <span class="badge-status badge bg-<%= pg.status === 'published' ? 'success' : 'warning' %>">
                      <%= pg.status %>
                    </span>
                  </p>
                  <p><strong>Verified:</strong> 
                    <span class="badge-status badge bg-<%= pg.verified ? 'success' : 'secondary' %>">
                      <%= pg.verified ? 'Verified' : 'Not Verified' %>
                    </span>
                  </p>
                  <p><strong>Created:</strong> <%= pg.createdAtFormatted %></p>
                </div>
              </div>

              <% if (pg.amenities && pg.amenities.length > 0) { %>
                <div class="mt-3">
                  <strong>Amenities:</strong>
                  <div class="mt-2">
                    <% pg.amenities.forEach(amenity => { %>
                      <span class="amenity-tag"><i class="bi bi-check-circle text-success"></i> <%= amenity %></span>
                    <% }) %>
                  </div>
                </div>
              <% } %>

              <% if (pg.description) { %>
                <div class="mt-3">
                  <strong>Description:</strong>
                  <p class="mt-2"><%= pg.description %></p>
                </div>
              <% } %>
            </div>

            <!-- Booking History -->
            <div class="stat-card">
              <h5 class="mb-3">Booking History</h5>
              <% if (bookings.length > 0) { %>
                <% bookings.forEach(booking => { %>
                  <div class="booking-card">
                    <div class="row align-items-center">
                      <div class="col-md-2">
                        <img src="<%= booking.tenantAvatar || '/media/default-avatar.png' %>" class="rounded-circle" width="48" height="48">
                      </div>
                      <div class="col-md-4">
                        <strong><%= booking.tenantName %></strong><br>
                        <small class="text-muted"><%= booking.tenantPhone %></small><br>
                        <small class="text-muted"><%= booking.tenantEmail %></small>
                      </div>
                      <div class="col-md-2">
                        <small><strong>Check-in:</strong><br><%= new Date(booking.checkInDate).toLocaleDateString('en-IN') %></small>
                      </div>
                      <div class="col-md-2">
                        <small><strong>Check-out:</strong><br><%= new Date(booking.checkOutDate).toLocaleDateString('en-IN') %></small>
                      </div>
                      <div class="col-md-2 text-end">
                        <strong>₹<%= booking.monthlyRent %>/mo</strong><br>
                        <span class="badge-status badge bg-<%= 
                          booking.bookingStatus === 'completed' ? 'success' : 
                          booking.bookingStatus === 'confirmed' ? 'primary' : 
                          booking.bookingStatus === 'pending' ? 'warning' : 'danger' 
                        %>">
                          <%= booking.bookingStatus %>
                        </span>
                      </div>
                    </div>
                    <% if (booking.notes) { %>
                      <div class="mt-2">
                        <small class="text-muted"><strong>Notes:</strong> <%= booking.notes %></small>
                      </div>
                    <% } %>
                  </div>
                <% }) %>
              <% } else { %>
                <p class="text-muted">No bookings yet</p>
              <% } %>
            </div>
          </div>

          <!-- Right Column - Revenue -->
          <div class="col-lg-4">
            <!-- Revenue -->
            <div class="stat-card mb-4">
              <h5 class="mb-3">Revenue Details</h5>
              <% if (payments.length > 0) { %>
                <% payments.forEach(payment => { %>
                  <div class="d-flex justify-content-between align-items-center mb-3 pb-2 border-bottom">
                    <div>
                      <div class="fw-semibold"><%= payment.userName %></div>
                      <small class="text-muted"><%= new Date(payment.createdAt).toLocaleDateString('en-IN') %></small>
                    </div>
                    <div class="text-end">
                      <div class="fw-bold">₹<%= payment.ownerAmount.toLocaleString('en-IN') %></div>
                      <small class="badge-status badge bg-<%= payment.status === 'completed' ? 'success' : 'warning' %>">
                        <%= payment.status %>
                      </small>
                    </div>
                  </div>
                <% }) %>
              <% } else { %>
                <p class="text-muted">No revenue data available</p>
              <% } %>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <div class="footer text-center">
    © <%= new Date().getFullYear() %> FindMyPG Owner. All rights reserved.
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function changeMainImage(url) {
      document.getElementById('mainImage').src = url;
    }

    const toggleBtn = document.getElementById('sidebarToggle');
    const sidebar = document.querySelector('.sidebar');
    const overlay = document.getElementById('sidebarOverlay');

    if (toggleBtn && sidebar && overlay) {
      toggleBtn.addEventListener('click', () => {
        sidebar.classList.toggle('show');
        overlay.classList.toggle('show');
      });

      overlay.addEventListener('click', () => {
        sidebar.classList.remove('show');
        overlay.classList.remove('show');
      });
    }
  </script>
</body>

</html>