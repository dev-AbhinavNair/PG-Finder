<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FindMyPG Owner - Payout Settings</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
    />

    <style>
      body {
        background: #f5f7fb;
      }
      .topbar {
        background: #ffffff;
        border-bottom: 1px solid #e5e7eb;
      }
      .brand-dot {
        width: 28px;
        height: 28px;
        border-radius: 8px;
        background: #0ea5a4;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-weight: 700;
      }
      .sidebar {
        background: #f3f4f6;
        border-right: 1px solid #e5e7eb;
        min-height: calc(100vh - 56px);
        padding: 14px 10px;
      }
      .side-link {
        display: flex;
        align-items: center;
        gap: 10px;
        color: #374151;
        text-decoration: none;
        padding: 10px 12px;
        border-radius: 10px;
        font-size: 0.95rem;
      }
      .side-link:hover {
        background: #e9eefb;
      }
      .side-link.active {
        background: #e9eefb;
        color: #111827;
        font-weight: 600;
      }
      .side-ic {
        width: 18px;
        height: 18px;
        opacity: 0.75;
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }
      .content-wrap {
        padding: 18px 18px 28px;
      }
      .page-title {
        font-weight: 700;
        font-size: 1.4rem;
        color: #111827;
      }
      .section-card {
        background: #fff;
        border-radius: 12px;
        padding: 22px;
        border: 1px solid #e5e7eb;
        margin-bottom: 18px;
      }
      .section-title {
        font-weight: 600;
        font-size: 1.05rem;
        color: #111827;
        margin-bottom: 16px;
      }
      .section-desc {
        color: #6b7280;
        font-size: 0.9rem;
        margin-bottom: 14px;
      }
      .form-group {
        margin-bottom: 16px;
      }
      .form-label {
        display: block;
        color: #374151;
        font-weight: 500;
        font-size: 0.9rem;
        margin-bottom: 6px;
      }
      .form-control {
        width: 100%;
        padding: 10px 12px;
        border: 1px solid #d1d5db;
        border-radius: 8px;
        font-size: 0.9rem;
        color: #111827;
      }
      .form-control:focus {
        outline: none;
        border-color: #0ea5a4;
        box-shadow: 0 0 0 3px rgba(14, 165, 164, 0.1);
      }
      .btn-primary-teal {
        background: #0ea5a4;
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 10px 20px;
        font-weight: 500;
        cursor: pointer;
      }
      .btn-primary-teal:hover {
        background: #0d9a94;
      }
      .btn-secondary {
        background: #fff;
        border: 1px solid #d1d5db;
        color: #374151;
        border-radius: 8px;
        padding: 10px 20px;
        font-weight: 500;
        cursor: pointer;
        text-decoration: none;
      }
      .btn-secondary:hover {
        background: #f9fafb;
      }
      .footer {
        color: #9ca3af;
        font-size: 0.85rem;
        padding: 18px;
        border-top: 1px solid #e5e7eb;
        background: #fff;
      }
      .alert {
        padding: 12px 16px;
        border-radius: 8px;
        margin-bottom: 16px;
      }
      .alert-success {
        background: #d1f4e0;
        color: #047857;
        border: 1px solid #6ee7b7;
      }
      .alert-error {
        background: #fee2e2;
        color: #991b1b;
        border: 1px solid #fca5a5;
      }
      .back-link {
        color: #0ea5a4;
        text-decoration: none;
        font-size: 0.9rem;
        margin-bottom: 16px;
        display: inline-block;
      }
    </style>
  </head>

  <body>
    <nav class="navbar topbar navbar-expand-lg px-3" style="height: 56px">
      <div class="d-flex align-items-center gap-2">
        <div class="brand-dot">🏠</div>
        <span class="fw-semibold" style="color: #0f766e">FindMyPG Owner</span>
      </div>

      <div class="ms-auto d-flex align-items-center gap-3">
        <% if (typeof owner !=="undefined" && owner) { %>
        <span class="small text-secondary d-none d-md-inline"
          >Hi, <%= owner.name %></span
        >
        <% } %>

        <div
          class="rounded-circle bg-light border"
          style="width: 34px; height: 34px; overflow: hidden"
        >
          <% if (typeof owner !=="undefined" && owner && owner.avatar_url) { %>
          <img
            src="<%= owner.avatar_url %>"
            alt="avatar"
            style="width: 100%; height: 100%; object-fit: cover"
          />
          <% } %>
        </div>
      </div>
    </nav>

    <div class="container-fluid">
      <div class="row g-0">
        <aside class="col-12 col-md-3 col-lg-2 sidebar">
          <a class="side-link" href="/owner">
            <span class="side-ic">▦</span> My PGs
          </a>
          <a class="side-link" href="/owner/pg/new">
            <span class="side-ic">＋</span> Add New PG
          </a>
          <a class="side-link" href="/owner/bookings">
            <span class="side-ic">🗓</span> Bookings
          </a>
          <a class="side-link" href="/owner/messages">
            <span class="side-ic">💬</span> Messages
          </a>
          <a class="side-link active" href="/owner/payouts">
            <span class="side-ic">₹</span> Payouts
          </a>
          <a class="side-link" href="/owner/settings">
            <span class="side-ic"><i class="bi bi-gear"></i></span> Settings
          </a>

          <form action="/logout" method="POST" class="mt-3 px-2">
            <button
              class="btn btn-outline-secondary w-100 btn-sm"
              type="submit"
            >
              Logout
            </button>
          </form>
        </aside>

        <main class="col-12 col-md-9 col-lg-10 content-wrap">
          <a href="/owner/payouts" class="back-link">← Back to Payouts</a>

          <div class="page-title mb-4">Payout Settings</div>

          <% if (typeof success !== 'undefined' && success) { %>
          <div class="alert alert-success"><%= success %></div>
          <% } %> <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-error"><%= error %></div>
          <% } %>

          <form method="POST" action="/owner/payouts/settings">
            <div class="section-card">
              <div class="section-title">Bank Account Details</div>
              <div class="section-desc">
                Add or update your bank details to receive payouts.
              </div>

              <div class="form-group">
                <label class="form-label">Account Holder Name</label>
                <input
                  type="text"
                  class="form-control"
                  name="account_holder"
                  value="<%= owner && owner.bank_account_holder ? owner.bank_account_holder : '' %>"
                  required
                />
              </div>

              <div class="form-group">
                <label class="form-label">Bank Name</label>
                <input
                  type="text"
                  class="form-control"
                  name="bank_name"
                  placeholder="e.g., HDFC Bank, ICICI Bank"
                  value="<%= owner && owner.bank_name ? owner.bank_name : '' %>"
                  required
                />
              </div>

              <div class="form-group">
                <label class="form-label">Account Number</label>
                <input
                  type="text"
                  class="form-control"
                  name="account_number"
                  placeholder="e.g., 1234567890123456"
                  value="<%= owner && owner.bank_account_number ? owner.bank_account_number : '' %>"
                  required
                />
              </div>

              <div class="form-group">
                <label class="form-label">IFSC Code</label>
                <input
                  type="text"
                  class="form-control"
                  name="ifsc_code"
                  placeholder="e.g., HDFC0000001"
                  value="<%= owner && owner.bank_ifsc_code ? owner.bank_ifsc_code : '' %>"
                  required
                />
              </div>
            </div>

            <div class="section-card">
              <div class="section-title">UPI Details (Optional)</div>
              <div class="section-desc">Add UPI ID for faster payouts.</div>

              <div class="form-group">
                <label class="form-label">UPI ID</label>
                <input
                  type="text"
                  class="form-control"
                  name="upi_id"
                  placeholder="e.g., yourname@upi"
                  value="<%= owner && owner.upi_id ? owner.upi_id : '' %>"
                />
              </div>
            </div>

            <div
              class="section-card"
              style="background: #f9fafb; border-left: 4px solid #0ea5a4"
            >
              <div style="color: #374151; font-weight: 500; margin-bottom: 8px">
                ⚠️ Important
              </div>
              <div style="color: #6b7280; font-size: 0.9rem">
                • Ensure all bank details are correct before requesting
                payouts<br />
                • Payouts are processed every 15 days<br />
                • Minimum payout amount is ₹100<br />
                • Bank transfers take 2-3 business days
              </div>
            </div>

            <div
              style="
                display: flex;
                gap: 12px;
                margin-top: 24px;
                margin-bottom: 40px;
              "
            >
              <button type="submit" class="btn-primary-teal">
                Save Bank Details
              </button>
              <a href="/owner/payouts" class="btn-secondary">Cancel</a>
            </div>
          </form>
        </main>
      </div>
    </div>

    <div class="footer text-center">
      © <%= new Date().getFullYear() %> FindMyPG Owner. All rights reserved.
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
  function showToast(message, type = 'success', title = 'Success') {
    const toastHTML = `
      <div class="toast align-items-center text-white bg-${type === 'success' ? 'success' : 'danger'} border-0" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
          <div class="toast-body">
            <strong>${title}</strong><br>${message}
          </div>
          <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
      </div>
    `;

    const toastContainer = document.getElementById('toastContainer') || createToastContainer();
    const toastElement = document.createElement('div');
    toastElement.innerHTML = toastHTML;
    toastContainer.appendChild(toastElement.firstElementChild);

    const toast = new bootstrap.Toast(toastContainer.lastElementChild);
    toast.show();
  }

  function createToastContainer() {
    const container = document.createElement('div');
    container.id = 'toastContainer';
    container.style.cssText = 'position: fixed; top: 20px; right: 20px; z-index: 9999;';
    document.body.appendChild(container);
    return container;
  }

  const params = new URLSearchParams(window.location.search);
  if (params.has('success')) {
    showToast(params.get('success'), 'success', '✓ Success');
    window.history.replaceState({}, document.title, window.location.pathname);
  }
  if (params.has('error')) {
    showToast(params.get('error'), 'error', '✕ Error');
    window.history.replaceState({}, document.title, window.location.pathname);
  }

  document.addEventListener('submit', (e) => {
    const form = e.target;
    const btn = form.querySelector('button[type="submit"]');
    if (btn && !form.hasAttribute('data-no-loading')) {
      const originalHTML = btn.innerHTML;
      btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Processing...';
      btn.disabled = true;
      
      setTimeout(() => {
        btn.innerHTML = originalHTML;
        btn.disabled = false;
      }, 500);
    }
  });
</script>

  </body>
</html>
